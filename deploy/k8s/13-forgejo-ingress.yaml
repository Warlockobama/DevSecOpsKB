# OPTIONAL external access to the Forgejo UI. Disabled by default (the demo runs
# fully in-cluster). Uncomment and adjust host/class/TLS for your environment.
#
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: forgejo
#   namespace: devsecops-kb
#   labels:
#     app.kubernetes.io/part-of: devsecops-kb
#     app.kubernetes.io/name: forgejo
# spec:
#   ingressClassName: nginx
#   rules:
#     - host: forge.example.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: forgejo
#                 port:
#                   number: 3000
